name: Release to crates.io

# This workflow ONLY runs when a new version tag is pushed (e.g. v1.0.5)
# It will NOT run on regular commits to main or dev.
# To release: merge dev -> main, bump versions, push tag v1.X.X

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

defaults:
  run:
    working-directory: Rullst

jobs:
  # Step 1: Run full test suite before publishing anything
  verify:
    name: Verify before release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Rullst
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          path: Rullst

      - name: Checkout rullst-orm
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          repository: venelouis/rullst-orm
          path: rullst-orm

      - name: Set up Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@46268bd060767258de96ed93c1251119784f2ab6 # v1
        with:
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt --package rullst --package rullst-macros --package cargo-rullst --package rullst-blog-example -- --check

      - name: Run Clippy (strict)
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Run all workspace tests
        run: cargo test --workspace

  # Step 2: Publish to crates.io only if all tests pass
  publish:
    name: Publish to crates.io
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Rullst
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          path: Rullst

      - name: Checkout rullst-orm
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          repository: venelouis/rullst-orm
          path: rullst-orm

      - name: Set up Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@46268bd060767258de96ed93c1251119784f2ab6 # v1

      - name: Publish rullst-macros
        run: cargo publish -p rullst-macros --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

      # Wait for crates.io to index rullst-macros before publishing rullst
      - name: Wait for rullst-macros to be indexed
        run: sleep 30

      - name: Publish rullst
        run: cargo publish -p rullst --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

      # Wait for crates.io to index rullst before publishing cargo-rullst
      - name: Wait for rullst to be indexed
        run: sleep 30

      - name: Publish cargo-rullst
        run: cargo publish -p cargo-rullst --token ${{ secrets.CARGO_REGISTRY_TOKEN }}
