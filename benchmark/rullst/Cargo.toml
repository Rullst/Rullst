[package]
name = "bench-rullst"
version = "0.1.0"
edition = "2024"

[dependencies]
rullst = { path = "../../rullst" }
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1.52.3", features = ["macros", "rt-multi-thread"] }

[workspace]
