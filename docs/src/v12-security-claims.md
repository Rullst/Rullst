# v12 security claims and evidence ledger

This page is the canonical boundary for security claims made by the Rullst v12
release notes. Each row names the narrow behavior implemented in the repository,
the tests that exercise it, and a known limit. A security statement that is not
represented here is not a v12 release guarantee.

The evidence links point to source-controlled tests. A passing local run proves
only that checkout and environment; the release candidate must attach the same
checks to its immutable tag SHA. None of these rows is a certification, external
audit, proof of complete OWASP coverage, or substitute for an application's own
threat model.

| Narrow v12 claim | Implementation | Verification | Known boundary |
| --- | --- | --- | --- |
| Nexus requires an explicit access policy and rejects an unverified peer. | [`NexusAuthPolicy`](../../rullst-nexus/src/nexus/access.rs) | [Access-policy unit tests](../../rullst-nexus/src/nexus/access/tests.rs) | Loopback convenience is debug-only. Basic Auth requires verified TLS, and independent review of the final deployment remains open. |
| Studio's built-in local capability accepts only a verified loopback peer in debug builds. | [`LocalStudioAccess`](../../rullst-studio/src/access.rs) | [Inline positive, remote-peer, and missing-peer tests](../../rullst-studio/src/access.rs) | Shared or release deployments need an application-owned authenticated boundary; Studio does not supply one. |
| Newly generated CORS middleware fails closed without a valid explicit origin allowlist. | [CORS scaffold template](../../cargo-rullst/src/generators/cors_middleware.rs.template) | [Generated-middleware HTTP tests](../../cargo-rullst/tests/cors_scaffold_tests.rs) | Previously generated applications are copied code and are not rewritten by a CLI upgrade. Browser/proxy behavior must still be tested in the deployed application. |
| Core CSRF middleware uses a double-submit check and only exact configured webhook exemptions. | [CSRF middleware](../../rullst-core/src/security/csrf.rs) | [CSRF unit tests](../../rullst-core/src/security/csrf.rs) and [application integration tests](../../rullst/tests/security_tests.rs) | Safe deployment still depends on HTTPS, cookie configuration, application authentication, and a correct exact-path exemption list. Request-body inspection is bounded. |
| Core WAF inspects and preserves supported bounded request bodies and rejects bodies outside its one-megabyte inspection envelope. | [WAF middleware](../../rullst-core/src/security/waf.rs) | [WAF body and query tests](../../rullst-core/src/security/tests.rs) | Detection is pattern-based, has false positives and negatives, and is not complete OWASP coverage. Streaming or other uninspected representations require application controls. |
| Parameterized routes recognized by the CLI audit require an adjacent public/owner/role/admin classification; owner paths require the recognized ownership guard. | [AST route audit](../../cargo-rullst/src/generators/audit.rs) and [`RbacGuard`](../../rullst-security/src/rbac/guard.rs) | [Audit negatives](../../cargo-rullst/src/generators/audit.rs), [scaffold contracts](../../cargo-rullst/tests/scaffold_contracts.rs), and [HTTP owner/cross-owner test](../../rullst-security/tests/security_tests.rs) | Static analysis recognizes bounded syntax and guard names; it cannot prove the correctness of an application's identity source, object lookup, or business authorization. |
| Encrypted session tokens are versioned, authenticated with AES-256-GCM, expire, and reject malformed or weak-key inputs. | [Session implementation](../../rullst-auth/src/auth.rs) | [Session negative and round-trip tests](../../rullst-auth/src/auth.rs) and [integration tests](../../rullst-auth/tests/integration_test.rs) | Application-key custody, rotation, session/device revocation, and distributed logout are application or roadmap responsibilities. |
| The current passkey foundation binds ceremonies to relying-party/origin data, consumes bounded challenges once, verifies flags/signatures, and enforces monotonic counters. | [Passkey service](../../rullst-auth/src/auth/passkey/service.rs) and [ceremony store](../../rullst-auth/src/auth/passkey/ceremony.rs) | [Invariant tests](../../rullst-auth/src/auth/passkey/invariant_tests.rs) and [negative-path tests](../../rullst-auth/src/auth/passkey/service.rs) | It has not passed an external WebAuthn conformance suite or independent implementation audit and must not be advertised as complete conformance. |
| Capital webhook middleware bounds payloads at two megabytes, verifies the selected provider before dispatch, checks freshness, and rejects replay within its configured store. | [Webhook middleware and replay store](../../rullst-capital/src/webhook.rs) | [Freshness, reconstruction, capacity, expiry, and replay tests](../../rullst-capital/src/webhook.rs) | The default replay store is process-local. Multi-instance deployments require durable shared idempotency, provider-specific operational validation, and an append-only audit trail. |
| Vault field envelopes use authenticated AES-256-GCM, random nonces, version/key identifiers, AAD binding, and explicit rotation keys. | [Vault implementation](../../rullst-security/src/vault.rs) | [Round-trip, tamper, wrong-key, malformed-envelope, and rotation tests](../../rullst-security/src/vault.rs) | The caller owns key custody and rotation. No real KMS/HSM adapter or hardware-backed key claim is made. |
| OTA accepts firmware for commit only after Ed25519 verification binds target, version, rollback counter, length, and SHA-256 digest. | [OTA manifest verifier](../../rullst-iot/src/ota.rs) | [OTA integration negatives and state tests](../../rullst-iot/tests/iot_integration_tests.rs) and [bounded fuzz target](../../rullst-iot/fuzz/fuzz_targets/fuzz_ota.rs) | Rullst does not download, flash, boot, or durably persist the rollback counter. Platform bootloader and storage integration remain external. |
| Core and extended secure-header layers share one per-request CSP nonce and emit the configured header baseline. | [Core header layer](../../rullst-core/src/security/headers.rs) and [extended layer](../../rullst-security/src/headers.rs) | [Nonce identity, composition, and malformed-configuration tests](../../rullst-security/src/headers.rs) | A header set does not guarantee a scanner grade. CSP sources, TLS termination, proxy changes, browser behavior, and application HTML still require deployment testing. |
| Response DLP masks recognized secrets only in supported, bounded textual payloads without corrupting invalid UTF-8 or incomplete values. | [DLP middleware](../../rullst-security/src/dlp.rs) | [Unit/integration tests](../../rullst-security/tests/rasp_waf_dlp_test.rs) and [bounded fuzz target](../../rullst-security/fuzz/fuzz_targets/fuzz_dlp.rs) | It does not prove zero leakage and does not rewrite arbitrary binary, compressed, encrypted, or streaming bodies. Unknown secret formats can pass through. |
| RASP rejects the implemented SQL injection, path traversal, JNDI, SSRF, RCE, header, and decoded JSON patterns within its inspection boundary. | [RASP inspector](../../rullst-security/src/rasp.rs) | [Middleware and pattern tests](../../rullst-security/src/rasp.rs), [integration tests](../../rullst-security/tests/rasp_waf_dlp_test.rs), and [bounded fuzz target](../../rullst-security/fuzz/fuzz_targets/fuzz_rasp.rs) | It is a bounded detector, not a parser for every protocol or proof against every bypass; false positives and false negatives remain possible. |
| Login Jail applies bounded progressive delay and temporary process-local jailing, with cleanup of expired state. | [Login guard](../../rullst-security/src/login_guard.rs) | [Timing, jail, expiry, capacity, and concurrency tests](../../rullst-security/src/login_guard.rs) and [contention tests](../../rullst-security/tests/concurrency_tests.rs) | State is not distributed. Correct client identity depends on trusted peer/proxy handling, and deployments still need upstream abuse controls. |
| Audit-chain entries use HMAC verification and unambiguous length-prefixing; local unsigned telemetry is not presented as HMAC verified. | [Audit chain](../../rullst-security/src/audit/chain.rs), [telemetry store](../../rullst-security/src/telemetry.rs), and [Nexus rendering](../../rullst-nexus/src/nexus/security.rs) | [Tamper/concurrency tests](../../rullst-security/tests/concurrency_tests.rs), [telemetry provenance tests](../../rullst-security/src/telemetry.rs), and [Nexus badge test](../../rullst-nexus/src/nexus/security.rs) | In-memory/local telemetry is not durable SIEM delivery. Key management, external storage, retry, dead-letter handling, and independent verification remain open. |
| Local security telemetry uses an exact, versioned v1 JSON envelope with normalized identifiers, IPs and timestamps, bounded UTF-8 details, and escaped CEF extension values. | [`LiveSecurityEvent`](../../rullst-security/src/telemetry/event.rs), [machine-readable schema](../../rullst-security/schema/security-event-v1.schema.json), and [CEF serializer](../../rullst-security/src/siem.rs) | [Schema parity and adversarial normalization tests](../../rullst-security/src/telemetry/event.rs) and [CEF injection test](../../rullst-security/src/siem.rs) | The schema is an interoperability boundary only. It does not provide source authentication, delivery, acknowledgement, retention, correlation, retry, dead-letter handling, or an external SIEM. |
| AI prompt checks and PII masking run before built-in provider dispatch and report only a heuristic result. | [`GuardrailReport`](../../rullst-ai/src/ai/guardrails.rs) and [provider dispatch](../../rullst-ai/src/ai/providers/mod.rs) | [Cross-provider guardrail pipeline](../../rullst-ai/tests/guardrails_pipeline_test.rs) and [provider capability tests](../../rullst-ai/src/ai/providers/mod.rs) | Passing heuristics or JSON schema does not make output trustworthy, stop every injection, authorize tools, prevent egress, or prove an upstream request was cancelled. |
| Local AI tool dispatch requires an exact allowlist, principal authorization, closed bounded JSON, call budget and audit sink; destructive/financial approvals are one-use and exact-payload-bound. | [Guarded tool registry](../../rullst-ai/src/ai/tools.rs), [policy](../../rullst-ai/src/ai/tools/policy.rs), and [audit contract](../../rullst-ai/src/ai/tools/audit.rs) | [Authorization, schema, budget, approval binding, failure, and audit tests](../../rullst-ai/src/ai/tools/tests.rs) | Provider-native calling, principal/approver authentication, domain ownership, network egress policy and a durable production sink remain application or roadmap responsibilities. |
| Live Alipay RSA2 verification and NFS-e homologation/production fail closed instead of returning simulated success. | [Alipay adapter](../../rullst-capital/src/providers/alipay.rs) and [NFS-e client](../../rullst-capital/src/fiscal/client.rs) | [Alipay unsupported-path tests](../../rullst-capital/src/providers/alipay.rs) and [fiscal environment tests](../../rullst-capital/src/fiscal/client.rs) | Offline fixtures are not live integrations, legal compliance, XMLDSig, mTLS, tax authorization, settlement, or provider homologation. |

## Release use

Before an RC or stable release, reviewers should:

1. run the linked tests through the workspace trifecta on the candidate SHA;
2. attach multi-OS CI and packaged-distribution evidence to that same SHA;
3. confirm the changelog uses the narrow claim wording above;
4. keep every remaining boundary visible in the release notes; and
5. leave the release at `NO-GO` if a claimed control lacks matching evidence.

Threat assumptions and abuse cases live in the [v12 threat models](threat-models.md).
The broader implementation/roadmap boundary lives in the
[capability ledger](capability-ledger.md).
