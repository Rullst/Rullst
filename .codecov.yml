coverage:
  status:
    project:
      default:
        # basic
        target: auto
        threshold: 5%
    patch:
      default:
        target: 0%
        if_ci_failed: error # (default = false)

ignore:
  - "docs"
  - "examples"
